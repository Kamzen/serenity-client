import { createTheme } from "@mui/material";

const Theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#008d8a"
    }
  },
  typography: {
    fontSize: 12
  }
});

export default Theme;
